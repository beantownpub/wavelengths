image:
  name: wavelengths
  repository: jalgraves/wavelengths
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

containerPort: 3077
regionCode:
resources:
  requests:
    cpu: 50m
    memory: 100Mi
  limits:
    memory: 350Mi
